import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-sandbox',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
